options {
    directory "/var/cache/bind";
    listen-on { any; };
    listen-on-v6 { any; };
    allow-query { any; };
    recursion no;
    dnssec-validation auto;
    // M-Eyes DNS firewall: response-policy is generated alongside zones.conf
    include "/etc/bind/m-eyes/rpz-options.conf";
};
